%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @following.each do |follower|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest('knwang@gmail.com'.downcase)}?s=40")
            = link_to follower.full_name, follower
          %td.extra-padding follower.queue_items.count
          %td.extra-padding follower.followed_user_relationships.count
          %td.extra-padding
            = link_to "", method: :post do
              %i.glyphicon.glyphicon-remove